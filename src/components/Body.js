import React from 'react';

export const Body = (props) =>(
    <div>
        <p className="App-intro">{props.title.join(', ')}</p>
    </div>
);

export const Body2 = (props)=>(
    <p>Samulel eto</p>
);

